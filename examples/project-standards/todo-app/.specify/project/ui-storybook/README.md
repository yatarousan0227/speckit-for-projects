# Project UI Storybook

This sample captures the project-level UI foundation for the ToDo application's local design system.

Use it to review Atomic Design parts and major screen layouts before creating feature-specific design bundles.

## Purpose

- Define reusable `atoms`, `molecules`, `organisms`, `templates`, and `pages`
- Review major screen layouts before feature-level design starts
- Keep local HTML mocks and Storybook stories aligned with `.general_sdd/project/design-system.md`
