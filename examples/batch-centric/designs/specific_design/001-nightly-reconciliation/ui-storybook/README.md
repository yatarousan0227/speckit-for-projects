# UI Storybook

This Storybook bundle captures the reconciliation report review state for operations.

## Local Review

```bash
npm install
npm run storybook
```

To verify the bundle before handoff:

```bash
npm run build-storybook
```
