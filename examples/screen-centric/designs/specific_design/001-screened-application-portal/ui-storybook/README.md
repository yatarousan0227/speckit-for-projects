# UI Storybook

This Storybook bundle is the UI design source of truth for the application search screen.

## Local Review

```bash
npm install
npm run storybook
```

To verify the bundle before handoff:

```bash
npm run build-storybook
```
