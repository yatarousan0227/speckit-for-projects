brief_id: 001-screened-application-portal
design_id: 001-screened-application-portal
common_design_refs:
  - ref_id: CD-API-001
    kind: api
    usage: consume
    notes:
      - Reuse the shared application search contract from the review screen.
  - ref_id: CD-DATA-001
    kind: data
    usage: read
    notes:
      - Render shared application summary fields in the search result list.
  - ref_id: CD-MOD-001
    kind: module
    usage: invoke
    notes:
      - Delegate search orchestration to the shared application search service.
  - ref_id: CD-UI-001
    kind: ui
    usage: read
    notes:
      - Reuse the shared screen catalog for review screens.
  - ref_id: CD-UI-002
    kind: ui
    usage: read
    notes:
      - Reuse the shared navigation rules for list-to-detail transitions.
