# UI Storybook

This Storybook bundle captures API consumer review states for the customer profile lookup contract.

## Local Review

```bash
npm install
npm run storybook
```

To verify the bundle before handoff:

```bash
npm run build-storybook
```
